# Dependencies for the Vercel serverless function (api/telegram_webhook.py).
#
# Vercel's Python runtime installs from THIS file — it does not read
# pyproject.toml / poetry.lock. Keep it minimal: the webhook only needs to
# talk to Telegram (requests), read config.example.yml (PyYAML) and reach
# Turso (libsql-experimental).
#
# Deliberately EXCLUDED (they're only for the scanner / dashboard and would
# add ~120 MB to the function bundle, hurting cold starts):
#   beautifulsoup4  — HTML parsing, scanner-only (sources/*)
#   streamlit       — dashboard only
#   pandas, plotly  — dashboard only
#
# The scanner itself runs on GitHub Actions via Poetry, so pyproject.toml
# stays the source of truth there. When you add a dependency that the
# *webhook* path needs, add it here too.
requests>=2.31,<3.0
PyYAML>=6.0,<7.0
libsql-experimental>=0.0.55,<0.0.56
